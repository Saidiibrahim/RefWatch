# Context Window Prime

RUN:
    git ls-files
    tree -L 2 -a -I '.claude|.cursor|.gemini|.vscode|.git|.cursorrules|.mcp.json'

READ:
    /RefWatch Watch App
    /RefWatch Watch AppTests
    /RefWatch Watch AppUITests


REVIEW:
    recent commits (2 most recent) in the current branch and summarise what was done

