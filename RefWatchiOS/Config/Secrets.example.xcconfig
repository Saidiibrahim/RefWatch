/* Copy to Secrets.xcconfig (DO NOT COMMIT) */
// Local-only debug secret for OpenAI. Do not use for Release.
OPENAI_API_KEY = your-local-debug-openai-key

// Supabase publishable key and project URL for local development/testing.
// Compose URL without typing '//' to avoid .xcconfig comment parsing
SCHEME = https
HOST = your-supabase-host
SLASH = /
SUPABASE_URL = $(SCHEME):$(SLASH)$(SLASH)$(HOST)

// Prefer publishable key naming; keep ANON as an alias for compatibility
SUPABASE_PUBLISHABLE_KEY = your-supabase-publishable-key
SUPABASE_ANON_KEY = $(SUPABASE_PUBLISHABLE_KEY)

// Google Auth Client ID
// NOTE: This should match the CLIENT_ID value from GoogleService-Info.plist
// The implementation now prioritizes GoogleService-Info.plist over this value
GID_CLIENT_ID = your-google-auth-client-id

// Google Sign-In Reversed Client ID (for URL scheme)
// This is used for the OAuth callback URL scheme in Info.plist
// Format: com.googleusercontent.apps.YOUR_CLIENT_ID
// Get this from GoogleService-Info.plist under REVERSED_CLIENT_ID
GID_REVERSED_CLIENT_ID = com.googleusercontent.apps.your-client-id

